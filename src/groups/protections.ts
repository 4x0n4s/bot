import { APIEmbed, APIActionRowComponent, APIMessageActionRowComponent, } from 'discord-api-types/v10';
import { Endpoints, defaultColor } from 'lib/Constants';
import { Command, Event } from '@decorators';
import { request } from 'undici';

export default class {
    /*
    [Protect/AntiRaid Commands]
    */
}   